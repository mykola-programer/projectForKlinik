<?xml version="1.0" encoding="UTF-8"?>

<entity-mappings xmlns="http://java.sun.com/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="http://java.sun.com/xml/ns/persistence/orm http://java.sun.com/xml/ns/persistence/orm_2_0.xsd"
                 version="2.0">
    <!--<class>ua.nike.project.hibernate.entity.Manager</class>-->
    <!--<class>ua.nike.project.hibernate.entity.Surgeon</class>-->
    <!--<class>ua.nike.project.hibernate.entity.Accomodation</class>-->
    <!--<class>ua.nike.project.hibernate.entity.Visit</class>-->
    <!--<class>ua.nike.project.hibernate.entity.DatePlan/class>-->
    <!--<class>ua.nike.project.hibernate.entity.Client</class>-->
    <!--<class>ua.nike.project.hibernate.entity.OperationType</class>-->

<!--    <named-query name="Visit.findAllHospitalizationBeanByOperationDate">
        <query>
            SELECT new ua.nike.project.hibernate.model.HospitalizationBean(
            op.datePlan.id,
            hp.id, hp.numberOfPlace,
            p.id, p.surname, p.firstName, p.secondName, p.sex, p.status,
            op.id, op.numberOfOrder,
            pr.id, pr.name, op.eye, op.timeForCome,
            op.surgeon, op.manager, op.note )
            FROM Visit op
            INNER JOIN op.datePlan ods
            INNER JOIN op.client p
            LEFT JOIN op.operationType pr
            INNER JOIN op.hospitalization hp
            WHERE op.datePlan.day = :datePlan and hp.datePlan.id = op.datePlan.id and
            op.hospitalization is not null
            ORDER BY hp.numberOfPlace
        </query>
    </named-query>

    <named-query name="Visit.findAllNotHospitalizationBeanByOperationDate">
        <query>
            SELECT new ua.nike.project.hibernate.model.HospitalizationBean(
            op.datePlan.id,
            hp.id, hp.numberOfPlace,
            p.id, p.surname, p.firstName, p.secondName, p.sex, p.status,
            op.id, op.numberOfOrder,
            pr.id, pr.name, op.eye, op.timeForCome,
            op.surgeon, op.manager, op.note )
            FROM Visit op
            INNER JOIN op.datePlan ods
            INNER JOIN op.client p
            LEFT JOIN op.operationType pr
            LEFT JOIN op.hospitalization hp
            WHERE op.datePlan.day = :datePlan and
            op.hospitalization is null
            ORDER BY hp.numberOfPlace
        </query>
    </named-query>-->
</entity-mappings>